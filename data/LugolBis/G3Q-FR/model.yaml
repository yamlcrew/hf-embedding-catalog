_id: 6a53bc3c3a4b75151be8b191
id: LugolBis/G3Q-FR
private: false
pipeline_tag: feature-extraction
library_name: transformers
tags:
- transformers
- onnx
- safetensors
- gemma3_text
- text-generation
- Summarization
- feature-extraction
- fr
- dataset:CATIE-AQ/taln-archives_fr_prompt_keywords_extraction
- dataset:CATIE-AQ/wikinews-fr-100_fr_prompt_keywords_extraction
- dataset:CATIE-AQ/termith-eval_fr_prompt_keywords_extraction
- base_model:google/gemma-3-270m-it
- base_model:quantized:google/gemma-3-270m-it
- license:gpl-3.0
- text-embeddings-inference
- endpoints_compatible
- region:us
downloads: 58
likes: 0
modelId: LugolBis/G3Q-FR
author: LugolBis
sha: f40cb9caa99f2c07e4243da800d6c14dd3550882
lastModified: '2026-07-18T21:35:42.000Z'
gated: false
disabled: false
model-index: null
config:
  architectures:
  - Gemma3ForCausalLM
  model_type: gemma3_text
  tokenizer_config:
    bos_token: <bos>
    eos_token: <eos>
    mask_token: <mask>
    pad_token: <pad>
    unk_token: <unk>
    use_default_system_prompt: false
    chat_template: '{{ bos_token }} {%- if messages[0][''role''] == ''system'' -%} {%- if messages[0][''content''] is string
      -%} {%- set first_user_prefix = messages[0][''content''] + '' '' -%} {%- else -%} {%- set first_user_prefix = messages[0][''content''][0][''text'']
      + '' '' -%} {%- endif -%} {%- set loop_messages = messages[1:] -%} {%- else -%} {%- set first_user_prefix = '''' -%}
      {%- set loop_messages = messages -%} {%- endif -%} {%- for message in loop_messages -%} {%- if (message[''role''] ==
      ''user'') != (loop.index0 % 2 == 0) -%} {{ raise_exception(''Conversation roles must alternate user/assistant/user/assistant/...'')
      }} {%- endif -%} {%- if (message[''role''] == ''assistant'') -%} {%- set role = ''model'' -%} {%- else -%} {%- set role
      = message[''role''] -%} {%- endif -%} {{ ''<start_of_turn>'' + role + '' '' + (first_user_prefix if loop.first else
      '''') }} {%- if message[''content''] is string -%} {{ message[''content''] | trim }} {%- elif message[''content''] is
      iterable -%} {%- for item in message[''content''] -%} {%- if item[''type''] == ''image'' -%} {{ ''<start_of_image>''
      }} {%- elif item[''type''] == ''text'' -%} {{ item[''text''] | trim }} {%- endif -%} {%- endfor -%} {%- else -%} {{
      raise_exception(''Invalid content type'') }} {%- endif -%} {{ ''<end_of_turn>'' }} {%- endfor -%} {%- if add_generation_prompt
      -%} {{''<start_of_turn>model ''}} {%- endif -%}'
  chat_template_jinja: "{{ bos_token }}\n{%- if messages[0]['role'] == 'system' -%}\n    {%- if messages[0]['content'] is\
    \ string -%}\n        {%- set first_user_prefix = messages[0]['content'] + '\n\n' -%}\n    {%- else -%}\n        {%- set\
    \ first_user_prefix = messages[0]['content'][0]['text'] + '\n\n' -%}\n    {%- endif -%}\n    {%- set loop_messages = messages[1:]\
    \ -%}\n{%- else -%}\n    {%- set first_user_prefix = \"\" -%}\n    {%- set loop_messages = messages -%}\n{%- endif -%}\n\
    {%- for message in loop_messages -%}\n    {%- if (message['role'] == 'user') != (loop.index0 % 2 == 0) -%}\n        {{\
    \ raise_exception(\"Conversation roles must alternate user/assistant/user/assistant/...\") }}\n    {%- endif -%}\n   \
    \ {%- if (message['role'] == 'assistant') -%}\n        {%- set role = \"model\" -%}\n    {%- else -%}\n        {%- set\
    \ role = message['role'] -%}\n    {%- endif -%}\n    {{ '<start_of_turn>' + role + '\n' + (first_user_prefix if loop.first\
    \ else \"\") }}\n    {%- if message['content'] is string -%}\n        {{ message['content'] | trim }}\n    {%- elif message['content']\
    \ is iterable -%}\n        {%- for item in message['content'] -%}\n            {%- if item['type'] == 'image' -%}\n  \
    \              {{ '<start_of_image>' }}\n            {%- elif item['type'] == 'text' -%}\n                {{ item['text']\
    \ | trim }}\n            {%- endif -%}\n        {%- endfor -%}\n    {%- else -%}\n        {{ raise_exception(\"Invalid\
    \ content type\") }}\n    {%- endif -%}\n    {{ '<end_of_turn>\n' }}\n{%- endfor -%}\n{%- if add_generation_prompt -%}\n\
    \    {{'<start_of_turn>model\n'}}\n{%- endif -%}\n"
cardData:
  license: gpl-3.0
  datasets:
  - CATIE-AQ/taln-archives_fr_prompt_keywords_extraction
  - CATIE-AQ/wikinews-fr-100_fr_prompt_keywords_extraction
  - CATIE-AQ/termith-eval_fr_prompt_keywords_extraction
  language:
  - fr
  base_model:
  - google/gemma-3-270m-it
  pipeline_tag: feature-extraction
  library_name: transformers
  tags:
  - Summarization
transformersInfo:
  auto_model: AutoModelForCausalLM
  pipeline_tag: text-generation
  processor: AutoTokenizer
siblings:
- rfilename: .gitattributes
- rfilename: README.md
- rfilename: chat_template.jinja
- rfilename: config.json
- rfilename: generation_config.json
- rfilename: model.safetensors
- rfilename: onnx/model.onnx
- rfilename: onnx/model_fp16.onnx
- rfilename: onnx/model_quantized.onnx
- rfilename: tokenizer.json
- rfilename: tokenizer_config.json
spaces: []
createdAt: '2026-07-12T16:09:32.000Z'
safetensors:
  parameters:
    BF16: 268098176
  total: 268098176
usedStorage: 2414054613
downloadsAllTime: 58
inferenceProviderMapping: {}
configJson:
  _sliding_window_pattern: 6
  architectures:
  - Gemma3ForCausalLM
  attention_bias: false
  attention_dropout: 0.0
  attn_logit_softcapping: null
  bos_token_id: 2
  dtype: bfloat16
  eos_token_id: 1
  final_logit_softcapping: null
  head_dim: 256
  hidden_activation: gelu_pytorch_tanh
  hidden_size: 640
  initializer_range: 0.02
  intermediate_size: 2048
  layer_types:
  - sliding_attention
  - sliding_attention
  - sliding_attention
  - sliding_attention
  - sliding_attention
  - full_attention
  - sliding_attention
  - sliding_attention
  - sliding_attention
  - sliding_attention
  - sliding_attention
  - full_attention
  - sliding_attention
  - sliding_attention
  - sliding_attention
  - sliding_attention
  - sliding_attention
  - full_attention
  max_position_embeddings: 32768
  model_type: gemma3_text
  num_attention_heads: 4
  num_hidden_layers: 18
  num_key_value_heads: 1
  pad_token_id: 0
  query_pre_attn_scalar: 256
  rms_norm_eps: 1.0e-06
  rope_parameters:
    full_attention:
      rope_theta: 1000000.0
      rope_type: default
    sliding_attention:
      rope_theta: 10000.0
      rope_type: default
  sliding_window: 512
  tie_word_embeddings: true
  transformers_version: 5.13.0
  use_bidirectional_attention: false
  use_cache: false
  vocab_size: 262144
onnxFiles:
  onnx/model.onnx: 1406.9
  onnx/model_fp16.onnx: 1406.9
  onnx/model_quantized.onnx: 437.5
safetensorsSizeMb: 536.2
approxSizeMb: 536.2
fetchedAt: '2026-07-19T06:05:32.000Z'
