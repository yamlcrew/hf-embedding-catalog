_id: 6a418b38e46698a2cd793494
id: Remidesbois/f2llm-v2-160m-one-piece-retrieval
private: false
pipeline_tag: sentence-similarity
library_name: sentence-transformers
tags:
- sentence-transformers
- onnx
- safetensors
- qwen3
- feature-extraction
- text-embeddings
- retrieval
- transformers.js
- one-piece-indexer
- sentence-similarity
- base_model:codefuse-ai/F2LLM-v2-160M
- base_model:quantized:codefuse-ai/F2LLM-v2-160M
- text-embeddings-inference
- endpoints_compatible
- region:us
downloads: 0
likes: 0
modelId: Remidesbois/f2llm-v2-160m-one-piece-retrieval
author: Remidesbois
sha: f28b312986e8f2562184c06d433ed9108e619aca
lastModified: '2026-06-28T21:00:02.000Z'
gated: false
disabled: false
widgetData:
- source_sentence: That is a happy person
  sentences:
  - That is a happy dog
  - That is a very happy person
  - Today is a sunny day
model-index: null
config:
  architectures:
  - Qwen3Model
  model_type: qwen3
  tokenizer_config:
    bos_token: null
    eos_token: <|im_end|>
    pad_token: <|endoftext|>
    unk_token: null
  chat_template_jinja: "{%- if tools %}\r\n    {{- '<|im_start|>system\\n' }}\r\n    {%- if messages[0].role == 'system' %}\r\
    \n        {{- messages[0].content + '\\n\\n' }}\r\n    {%- endif %}\r\n    {{- \"# Tools\\n\\nYou may call one or more\
    \ functions to assist with the user query.\\n\\nYou are provided with function signatures within <tools></tools> XML tags:\\\
    n<tools>\" }}\r\n    {%- for tool in tools %}\r\n        {{- \"\\n\" }}\r\n        {{- tool | tojson }}\r\n    {%- endfor\
    \ %}\r\n    {{- \"\\n</tools>\\n\\nFor each function call, return a json object with function name and arguments within\
    \ <tool_call></tool_call> XML tags:\\n<tool_call>\\n{\\\"name\\\": <function-name>, \\\"arguments\\\": <args-json-object>}\\\
    n</tool_call><|im_end|>\\n\" }}\r\n{%- else %}\r\n    {%- if messages[0].role == 'system' %}\r\n        {{- '<|im_start|>system\\\
    n' + messages[0].content + '<|im_end|>\\n' }}\r\n    {%- endif %}\r\n{%- endif %}\r\n{%- set ns = namespace(multi_step_tool=true,\
    \ last_query_index=messages|length - 1) %}\r\n{%- for message in messages[::-1] %}\r\n    {%- set index = (messages|length\
    \ - 1) - loop.index0 %}\r\n    {%- if ns.multi_step_tool and message.role == \"user\" and message.content is string and\
    \ not(message.content.startswith('<tool_response>') and message.content.endswith('</tool_response>')) %}\r\n        {%-\
    \ set ns.multi_step_tool = false %}\r\n        {%- set ns.last_query_index = index %}\r\n    {%- endif %}\r\n{%- endfor\
    \ %}\r\n{%- for message in messages %}\r\n    {%- if message.content is string %}\r\n        {%- set content = message.content\
    \ %}\r\n    {%- else %}\r\n        {%- set content = '' %}\r\n    {%- endif %}\r\n    {%- if (message.role == \"user\"\
    ) or (message.role == \"system\" and not loop.first) %}\r\n        {{- '<|im_start|>' + message.role + '\\n' + content\
    \ + '<|im_end|>' + '\\n' }}\r\n    {%- elif message.role == \"assistant\" %}\r\n        {%- set reasoning_content = ''\
    \ %}\r\n        {%- if message.reasoning_content is string %}\r\n            {%- set reasoning_content = message.reasoning_content\
    \ %}\r\n        {%- else %}\r\n            {%- if '</think>' in content %}\r\n                {%- set reasoning_content\
    \ = content.split('</think>')[0].rstrip('\\n').split('<think>')[-1].lstrip('\\n') %}\r\n                {%- set content\
    \ = content.split('</think>')[-1].lstrip('\\n') %}\r\n            {%- endif %}\r\n        {%- endif %}\r\n        {%-\
    \ if loop.index0 > ns.last_query_index %}\r\n            {%- if loop.last or (not loop.last and reasoning_content) %}\r\
    \n                {{- '<|im_start|>' + message.role + '\\n<think>\\n' + reasoning_content.strip('\\n') + '\\n</think>\\\
    n\\n' + content.lstrip('\\n') }}\r\n            {%- else %}\r\n                {{- '<|im_start|>' + message.role + '\\\
    n' + content }}\r\n            {%- endif %}\r\n        {%- else %}\r\n            {{- '<|im_start|>' + message.role +\
    \ '\\n' + content }}\r\n        {%- endif %}\r\n        {%- if message.tool_calls %}\r\n            {%- for tool_call\
    \ in message.tool_calls %}\r\n                {%- if (loop.first and content) or (not loop.first) %}\r\n             \
    \       {{- '\\n' }}\r\n                {%- endif %}\r\n                {%- if tool_call.function %}\r\n             \
    \       {%- set tool_call = tool_call.function %}\r\n                {%- endif %}\r\n                {{- '<tool_call>\\\
    n{\"name\": \"' }}\r\n                {{- tool_call.name }}\r\n                {{- '\", \"arguments\": ' }}\r\n      \
    \          {%- if tool_call.arguments is string %}\r\n                    {{- tool_call.arguments }}\r\n             \
    \   {%- else %}\r\n                    {{- tool_call.arguments | tojson }}\r\n                {%- endif %}\r\n       \
    \         {{- '}\\n</tool_call>' }}\r\n            {%- endfor %}\r\n        {%- endif %}\r\n        {{- '<|im_end|>\\\
    n' }}\r\n    {%- elif message.role == \"tool\" %}\r\n        {%- if loop.first or (messages[loop.index0 - 1].role != \"\
    tool\") %}\r\n            {{- '<|im_start|>user' }}\r\n        {%- endif %}\r\n        {{- '\\n<tool_response>\\n' }}\r\
    \n        {{- content }}\r\n        {{- '\\n</tool_response>' }}\r\n        {%- if loop.last or (messages[loop.index0\
    \ + 1].role != \"tool\") %}\r\n            {{- '<|im_end|>\\n' }}\r\n        {%- endif %}\r\n    {%- endif %}\r\n{%- endfor\
    \ %}\r\n{%- if add_generation_prompt %}\r\n    {{- '<|im_start|>assistant\\n' }}\r\n    {%- if enable_thinking is defined\
    \ and enable_thinking is false %}\r\n        {{- '<think>\\n\\n</think>\\n\\n' }}\r\n    {%- endif %}\r\n{%- endif %}"
cardData:
  base_model: codefuse-ai/F2LLM-v2-160M
  library_name: sentence-transformers
  pipeline_tag: sentence-similarity
  tags:
  - sentence-transformers
  - feature-extraction
  - text-embeddings
  - retrieval
  - transformers.js
  - onnx
  - one-piece-indexer
transformersInfo:
  auto_model: AutoModel
  pipeline_tag: feature-extraction
  processor: AutoTokenizer
siblings:
- rfilename: .gitattributes
- rfilename: 1_Pooling/config.json
- rfilename: README.md
- rfilename: chat_template.jinja
- rfilename: config.json
- rfilename: config_sentence_transformers.json
- rfilename: model.safetensors
- rfilename: modules.json
- rfilename: onnx/model.onnx
- rfilename: sentence_bert_config.json
- rfilename: tokenizer.json
- rfilename: tokenizer_config.json
- rfilename: training_manifest.json
spaces: []
createdAt: '2026-06-28T20:59:36.000Z'
safetensors:
  parameters:
    F32: 159185024
  total: 159185024
usedStorage: 1285260285
downloadsAllTime: 0
inferenceProviderMapping: {}
configJson:
  architectures:
  - Qwen3Model
  attention_bias: false
  attention_dropout: 0.0
  bos_token_id: 151643
  dtype: float32
  eos_token_id: 151645
  head_dim: 128
  hidden_act: silu
  hidden_size: 640
  initializer_range: 0.02
  intermediate_size: 1536
  layer_types:
  - full_attention
  - full_attention
  - full_attention
  - full_attention
  - full_attention
  - full_attention
  - full_attention
  - full_attention
  - full_attention
  max_position_embeddings: 40960
  max_window_layers: 9
  model_type: qwen3
  num_attention_heads: 16
  num_hidden_layers: 9
  num_key_value_heads: 8
  pad_token_id: null
  rms_norm_eps: 1.0e-06
  rope_parameters:
    rope_theta: 1000000
    rope_type: default
  sliding_window: null
  tie_word_embeddings: true
  transformers_version: 5.7.0
  use_cache: false
  use_sliding_window: false
  vocab_size: 151936
onnxFiles:
  onnx/model.onnx: 637.1
safetensorsSizeMb: 636.8
approxSizeMb: 636.7
fetchedAt: '2026-06-29T08:22:26.000Z'
